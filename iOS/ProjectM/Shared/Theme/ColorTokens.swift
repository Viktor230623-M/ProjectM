import SwiftUI

extension Color {
    static let mPrimary = Color("Primary")           // Rose — #E11D48 light / #FB7185 dark
    static let mAccent = Color("Accent")             // Blue — #2563EB light / #60A5FA dark
    static let mBackground = Color("Background")     // #FFFFFF / #000000
    static let mSurface = Color("Surface")           // #F5F5F5 / #111111
    static let mSurface2 = Color("Surface2")         // #EBEBEB / #1C1C1C
    static let mBorder = Color("Border")             // #E5E5E5 / #2A2A2A
    static let mTextPrimary = Color("TextPrimary")   // #0A0A0A / #F5F5F5
    static let mTextSecondary = Color("TextSecondary") // #6B6B6B / #9B9B9B
    static let mGraphNode = Color("GraphNode")       // #FB7185
    static let mGraphEdge = Color("GraphEdge")       // white 12% opacity
    static let mDestructive = Color("Destructive")   // #DC2626 / #F87171
}
